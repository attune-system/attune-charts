apiVersion: v1
kind: Service
metadata:
  name: {{ include "attune-site.fullname" . }}
  labels:
    {{- include "attune-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "attune-site.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
