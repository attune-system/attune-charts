apiVersion: v1
kind: Service
metadata:
  name: {{ include "attune.apiServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type | quote }}
  selector:
    {{- include "attune.componentLabels" (dict "root" . "component" "api") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "attune.apiServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.api.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "api") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "api") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-schema
          image: postgres:16-alpine
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" -tAc "SELECT to_regclass('${DB_SCHEMA}.identity')" | grep -q identity; do
                echo "waiting for schema";
                sleep 2;
              done
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
        - name: wait-for-packs
          image: busybox:1.36
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until [ -f /opt/attune/packs/core/pack.yaml ]; do
                echo "waiting for packs";
                sleep 2;
              done
          volumeMounts:
            - name: packs
              mountPath: /opt/attune/packs
        {{- if and .Values.rabbitmq.enabled .Values.rabbitmq.provisioning.enabled }}
        {{- include "attune.waitForRabbitmqCredentials" . | nindent 8 }}
        {{- else }}
        {{- include "attune.waitForRabbitmqPort" . | nindent 8 }}
        {{- end }}
      containers:
        - name: api
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.api) | quote }}
          imagePullPolicy: {{ .Values.images.api.pullPolicy | quote }}
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
            {{- if .Values.security.identitySecret.existingSecret }}
            - secretRef:
                name: {{ include "attune.identitySecretName" . | quote }}
            {{- end }}
          env:
            - name: ATTUNE_CONFIG
              value: /opt/attune/config.yaml
            - name: ATTUNE__DATABASE__SCHEMA
              value: {{ .Values.database.schema | quote }}
            - name: ATTUNE__WORKER__WORKER_TYPE
              value: container
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /opt/attune/config.yaml
              subPath: config.yaml
            - name: packs
              mountPath: /opt/attune/packs
            - name: runtime-envs
              mountPath: /opt/attune/runtime_envs
            - name: artifacts
              mountPath: /opt/attune/artifacts
      volumes:
        - name: config
          configMap:
            name: {{ printf "%s-config" (include "attune.fullname" .) | quote }}
        - name: packs
          persistentVolumeClaim:
            claimName: {{ printf "%s-packs" (include "attune.fullname" .) | quote }}
        - name: runtime-envs
          persistentVolumeClaim:
            claimName: {{ printf "%s-runtime-envs" (include "attune.fullname" .) | quote }}
        - name: artifacts
          persistentVolumeClaim:
            claimName: {{ printf "%s-artifacts" (include "attune.fullname" .) | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-executor" (include "attune.fullname" .) | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.executor.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "executor") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "executor") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-schema
          image: postgres:16-alpine
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" -tAc "SELECT to_regclass('${DB_SCHEMA}.identity')" | grep -q identity; do
                echo "waiting for schema";
                sleep 2;
              done
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
        {{- if and .Values.rabbitmq.enabled .Values.rabbitmq.provisioning.enabled }}
        {{- include "attune.waitForRabbitmqCredentials" . | nindent 8 }}
        {{- end }}
        - name: wait-for-packs
          image: busybox:1.36
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until [ -f /opt/attune/packs/core/pack.yaml ]; do
                echo "waiting for packs";
                sleep 2;
              done
          volumeMounts:
            - name: packs
              mountPath: /opt/attune/packs
      containers:
        - name: executor
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.executor) | quote }}
          imagePullPolicy: {{ .Values.images.executor.pullPolicy | quote }}
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
          env:
            - name: ATTUNE_CONFIG
              value: /opt/attune/config.yaml
            - name: ATTUNE__DATABASE__SCHEMA
              value: {{ .Values.database.schema | quote }}
            - name: ATTUNE__WORKER__WORKER_TYPE
              value: container
          resources:
            {{- toYaml .Values.executor.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /opt/attune/config.yaml
              subPath: config.yaml
            - name: packs
              mountPath: /opt/attune/packs
            - name: artifacts
              mountPath: /opt/attune/artifacts
      volumes:
        - name: config
          configMap:
            name: {{ printf "%s-config" (include "attune.fullname" .) | quote }}
        - name: packs
          persistentVolumeClaim:
            claimName: {{ printf "%s-packs" (include "attune.fullname" .) | quote }}
        - name: artifacts
          persistentVolumeClaim:
            claimName: {{ printf "%s-artifacts" (include "attune.fullname" .) | quote }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-supervisor" (include "attune.fullname" .) | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.supervisor.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "supervisor") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "supervisor") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-schema
          image: postgres:16-alpine
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" -tAc "SELECT to_regclass('${DB_SCHEMA}.identity')" | grep -q identity; do
                echo "waiting for schema";
                sleep 2;
              done
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
      containers:
        - name: supervisor
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.supervisor) | quote }}
          imagePullPolicy: {{ .Values.images.supervisor.pullPolicy | quote }}
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
          env:
            - name: ATTUNE_CONFIG
              value: /opt/attune/config.yaml
            - name: ATTUNE__DATABASE__SCHEMA
              value: {{ .Values.database.schema | quote }}
          resources:
            {{- toYaml .Values.supervisor.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /opt/attune/config.yaml
              subPath: config.yaml
            - name: artifacts
              mountPath: /opt/attune/artifacts
      volumes:
        - name: config
          configMap:
            name: {{ printf "%s-config" (include "attune.fullname" .) | quote }}
        - name: artifacts
          persistentVolumeClaim:
            claimName: {{ printf "%s-artifacts" (include "attune.fullname" .) | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "attune.notifierServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.notifier.service.type | quote }}
  selector:
    {{- include "attune.componentLabels" (dict "root" . "component" "notifier") | nindent 4 }}
  ports:
    - name: ws
      port: {{ .Values.notifier.service.port }}
      targetPort: ws
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "attune.notifierServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.notifier.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "notifier") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "notifier") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-schema
          image: postgres:16-alpine
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until PGPASSWORD="$DB_PASSWORD" psql -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" -d "$DB_NAME" -tAc "SELECT to_regclass('${DB_SCHEMA}.identity')" | grep -q identity; do
                echo "waiting for schema";
                sleep 2;
              done
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
      containers:
        - name: notifier
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.notifier) | quote }}
          imagePullPolicy: {{ .Values.images.notifier.pullPolicy | quote }}
          envFrom:
            - secretRef:
                name: {{ include "attune.secretName" . | quote }}
          env:
            - name: ATTUNE_CONFIG
              value: /opt/attune/config.yaml
            - name: ATTUNE__DATABASE__SCHEMA
              value: {{ .Values.database.schema | quote }}
            - name: ATTUNE__WORKER__WORKER_TYPE
              value: container
          ports:
            - name: ws
              containerPort: 8081
          readinessProbe:
            httpGet:
              path: /health
              port: ws
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: ws
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.notifier.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /opt/attune/config.yaml
              subPath: config.yaml
      volumes:
        - name: config
          configMap:
            name: {{ printf "%s-config" (include "attune.fullname" .) | quote }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-web" (include "attune.fullname" .) | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type | quote }}
  selector:
    {{- include "attune.componentLabels" (dict "root" . "component" "web") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-web" (include "attune.fullname" .) | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.web.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "web") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "web") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: web
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.web) | quote }}
          imagePullPolicy: {{ .Values.images.web.pullPolicy | quote }}
          env:
            - name: API_URL
              value: {{ .Values.web.config.apiUrl | quote }}
            - name: WS_URL
              value: {{ .Values.web.config.wsUrl | quote }}
            - name: ENVIRONMENT
              value: {{ .Values.web.config.environment | quote }}
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.web.resources | nindent 12 }}
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/attune.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: {{ printf "%s-config" (include "attune.fullname" .) | quote }}
{{- if .Values.mcp.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "attune.mcpServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  type: {{ .Values.mcp.service.type | quote }}
  selector:
    {{- include "attune.componentLabels" (dict "root" . "component" "mcp") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.mcp.service.port }}
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "attune.mcpServiceName" . | quote }}
  labels:
    {{- include "attune.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.mcp.replicaCount }}
  selector:
    matchLabels:
      {{- include "attune.componentLabels" (dict "root" . "component" "mcp") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "attune.componentLabels" (dict "root" . "component" "mcp") | nindent 8 }}
      annotations:
        attune.dev/release-revision: {{ .Release.Revision | quote }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-api
          image: busybox:1.36
          command: ["/bin/sh", "-ec"]
          args:
            - |
              until wget -qO- "http://{{ include "attune.apiServiceName" . }}:{{ .Values.api.service.port }}/health" >/dev/null; do
                echo "waiting for api";
                sleep 2;
              done
      containers:
        - name: mcp
          image: {{ include "attune.image" (dict "root" . "image" .Values.images.mcp) | quote }}
          imagePullPolicy: {{ .Values.images.mcp.pullPolicy | quote }}
          command: ["/usr/local/bin/attune-mcp"]
          env:
            - name: ATTUNE_API_URL
              value: {{ printf "http://%s:%v" (include "attune.apiServiceName" .) .Values.api.service.port | quote }}
            - name: ATTUNE_MCP_TRANSPORT
              value: http
            - name: ATTUNE_MCP_LISTEN_ADDR
              value: {{ printf "0.0.0.0:%v" .Values.mcp.service.port | quote }}
            {{- if .Values.mcp.auth.useBootstrapTestUser }}
            - name: ATTUNE_LOGIN
              valueFrom:
                secretKeyRef:
                  name: {{ include "attune.secretName" . | quote }}
                  key: "TEST_LOGIN"
            - name: ATTUNE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "attune.secretName" . | quote }}
                  key: "TEST_PASSWORD"
            {{- else }}
            - name: ATTUNE_LOGIN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.mcp.auth.existingSecret | quote }}
                  key: {{ .Values.mcp.auth.loginKey | quote }}
            - name: ATTUNE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.mcp.auth.existingSecret | quote }}
                  key: {{ .Values.mcp.auth.passwordKey | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.mcp.service.port }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            {{- toYaml .Values.mcp.resources | nindent 12 }}
{{- end }}
